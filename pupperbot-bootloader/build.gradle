group 'hu.suppoze.pupperbot.bootloader'
version '0.3'

mainClassName = "hu.suppoze.pupperbot.bootloader.BootLoader"

task copyResources(type: Copy) {
    from 'bootloader.json'
    into 'build/install/pupperbot-bootloader/bin'
}